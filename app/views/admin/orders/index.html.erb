<h2>注文一覧</h2>

<table>
  <tr>
    <th>購入日時</th>
    <th>名前</th>
    <th>注文ステータス</th>
  </tr>
  <% @orders.each do |order| %>
    <tr>
      <td><%= link_to order.created_at.strftime("%Y/%m/%d %H:%M:%S"), admin_order_path(order.id) %></td>
      <td><%= order.customer.last_name %> <%= order.customer.first_name %></td>
      <td><%= order.is_receive_i18n %></td>
    </tr>
  <% end %>
</table>